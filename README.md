# Codestra Keycloak

Git-managed configuration and deployment controls for the Codestra Keycloak service at `https://auth.codestra.co`.

The repository is initialized here so all subsequent Keycloak configuration changes can be reviewed through branches and pull requests. Runtime secrets, user credentials, database data, access tokens, and private keys must never be committed.
