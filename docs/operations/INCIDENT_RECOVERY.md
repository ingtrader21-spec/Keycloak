# Incident recovery

Preserve logs and recovery manifests without tokens. For a partial APPLY, stop further mutation, inspect operation states and corresponding rollback exports, generate a separately reviewed rollback plan, and revalidate live hashes before rollback. Newly created clients are never automatically deleted. Credential compromise follows the service-specific rotation runbook and requires coordinated consumer rollout.
