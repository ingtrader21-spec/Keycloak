# Protected apply secret names. Never load into the Keycloak runtime container
# or PostgreSQL; the reconciler sends them only to the reviewed realm endpoint.
KC_SMTP_USERNAME=<github-environment-secret:keycloak-smtp-username>
KC_SMTP_PASSWORD=<github-environment-secret:keycloak-smtp-password>
