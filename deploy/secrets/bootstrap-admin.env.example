# Inject only into Keycloak for initial bootstrap. Remove from the runtime
# secret provider after the permanent administration identity is verified.
KC_BOOTSTRAP_ADMIN_PASSWORD=<runtime-secret-provider:keycloak-bootstrap-password>
